# Sitio Web Forjadores de Leyendas — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Sitio estático en Astro que reemplaza el WordPress de forjadoresdeleyendas.mx, optimizado para convertir visitantes en reservas por WhatsApp.

**Architecture:** Astro 5 100% estático con content collections (blog en Markdown), Tailwind CSS 4 vía plugin de Vite, sin backend. Deploy: GitHub Action compila y publica `dist/` en la rama `hostinger-deploy`, que Hostinger sirve tal cual.

**Tech Stack:** Astro 5, Tailwind CSS 4, @astrojs/sitemap, @fontsource (Cinzel + Inter), sharp. Imágenes generadas con la API Gemini (Nano Banana).

## Global Constraints

- Spec: `docs/superpowers/specs/2026-07-30-sitio-web-design.md`. En conflicto, la spec manda.
- Idioma del sitio: español (México). `<html lang="es-MX">`.
- URL canónica: `https://forjadoresdeleyendas.mx`
- WhatsApp: `+52 2221 890232` → enlaces `https://wa.me/522221890232?text=<mensaje URL-encoded>`.
- Dirección: C. 12 Sur 908-1, Barrio de Analco, 72500 Puebla, Pue.
- Redes: X `https://x.com/Forjadleyendas`, YouTube `https://www.youtube.com/@ForjadoresdeLeyendas`, Instagram `https://www.instagram.com/forjadoresdeleyendas/`, TikTok `https://www.tiktok.com/@forjadoresdeleyendas_`, Twitch `https://www.twitch.tv/forjadoresdeleyendas`.
- Voz de marca: el posadero carismático — épico, cálido, accesible para novatos. Tutear al lector. Nada de texto de relleno.
- La palabra "Forjadores" SIEMPRE con la O (el typo "FORJADRES" de los letreros IA no debe entrar al sitio).
- Analytics: solo si hay ID configurado (`PUBLIC_GA4_ID`, `PUBLIC_CLARITY_ID` en `.env` — hoy vacíos).
- Paleta (CSS vars): pergamino `#F3E3CB`, pergamino-claro `#FAF3E3`, madera `#5C4020`, tinta `#2B1F14`, ámbar `#E8A33D`, fuego `#C4622D`, verde-escudo `#6B7A54`.
- Tipografía: Cinzel (títulos), Inter Variable (cuerpo). Self-hosted vía @fontsource.
- Verificación estándar de cada tarea: `npm run build` sin errores + greps sobre `dist/`.

---

### Task 1: Scaffold del proyecto Astro + organización del repo

**Files:**
- Create: `package.json`, `astro.config.mjs`, `tsconfig.json`, `src/styles/global.css`, `src/pages/index.astro` (provisional)
- Move: los archivos sueltos de marca (`*.docx`, `*.jpg`, `*.png`, `*.gif`) → `brand/originales/`

**Interfaces:**
- Produces: proyecto Astro compilable en la raíz del repo; `npm run build` genera `dist/`. Alias `@/*` → `src/*`. Tailwind 4 activo vía `@tailwindcss/vite`.

- [ ] **Step 1: Mover assets de marca**

```bash
cd "/Users/armandoaguilar/Downloads/ForjadoresDeLeyendas"
mkdir -p brand/originales
mv *.docx *.jpg *.png *.gif brand/originales/
```

- [ ] **Step 2: Crear package.json y configs**

`package.json`:
```json
{
  "name": "forjadoresdeleyendas",
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.2.0",
    "@fontsource-variable/inter": "^5.1.0",
    "@fontsource/cinzel": "^5.1.0",
    "@tailwindcss/vite": "^4.0.0",
    "astro": "^5.0.0",
    "sharp": "^0.33.0",
    "tailwindcss": "^4.0.0"
  }
}
```

`astro.config.mjs`:
```js
import { defineConfig } from 'astro/config';
import sitemap from '@astrojs/sitemap';
import tailwindcss from '@tailwindcss/vite';

export default defineConfig({
  site: 'https://forjadoresdeleyendas.mx',
  integrations: [sitemap()],
  vite: { plugins: [tailwindcss()] },
});
```

`tsconfig.json`:
```json
{
  "extends": "astro/tsconfigs/strict",
  "compilerOptions": {
    "baseUrl": ".",
    "paths": { "@/*": ["src/*"] }
  }
}
```

`src/styles/global.css` (tokens de la paleta; Tailwind 4 los expone como clases `bg-parchment`, `text-ink`, etc.):
```css
@import 'tailwindcss';

@theme {
  --color-parchment: #f3e3cb;
  --color-parchment-light: #faf3e3;
  --color-wood: #5c4020;
  --color-ink: #2b1f14;
  --color-amber: #e8a33d;
  --color-fire: #c4622d;
  --color-shield: #6b7a54;
  --font-display: 'Cinzel', serif;
  --font-body: 'Inter Variable', sans-serif;
}

html {
  scroll-behavior: smooth;
}

body {
  @apply bg-parchment text-ink font-body antialiased;
}
```

`src/pages/index.astro` (provisional, lo reemplaza la Task 4):
```astro
---
import '@/styles/global.css';
---
<html lang="es-MX"><head><title>Forjadores de Leyendas</title></head>
<body><h1>En construcción</h1></body></html>
```

- [ ] **Step 3: Instalar y verificar build**

```bash
npm install
npm run build
```
Expected: `npm run build` termina con "Complete!" y existe `dist/index.html`.

- [ ] **Step 4: Commit**

```bash
git add -A
git commit -m "feat: scaffold Astro + Tailwind, organizar assets de marca"
```

---

### Task 2: Layout base, componentes compartidos y analytics

**Files:**
- Create: `src/layouts/BaseLayout.astro`, `src/components/Header.astro`, `src/components/Footer.astro`, `src/components/WhatsAppButton.astro`, `src/components/Analytics.astro`, `src/lib/site.ts`, `.env.example`

**Interfaces:**
- Consumes: `global.css`, alias `@/*` (Task 1).
- Produces:
  - `BaseLayout` props: `{ title: string; description: string; ogImage?: string }`.
  - `WhatsAppButton` props: `{ message: string; label: string; variant?: 'primary' | 'outline' }`.
  - `src/lib/site.ts` exporta `SITE` (const con nombre, dirección, teléfono, redes) y `waLink(message: string): string`.

- [ ] **Step 1: Crear src/lib/site.ts**

```ts
export const SITE = {
  name: 'Forjadores de Leyendas',
  url: 'https://forjadoresdeleyendas.mx',
  description:
    'Posada temática en Puebla con sesiones privadas de Dungeons & Dragons y juegos de rol, guiadas por Guardianes de Historias, con comida temática y escenografía inmersiva.',
  phone: '+52 222 189 0232',
  whatsapp: '522221890232',
  address: {
    street: 'C. 12 Sur 908-1',
    neighborhood: 'Barrio de Analco',
    city: 'Puebla',
    state: 'Puebla',
    zip: '72500',
    country: 'MX',
  },
  social: {
    x: 'https://x.com/Forjadleyendas',
    youtube: 'https://www.youtube.com/@ForjadoresdeLeyendas',
    instagram: 'https://www.instagram.com/forjadoresdeleyendas/',
    tiktok: 'https://www.tiktok.com/@forjadoresdeleyendas_',
    twitch: 'https://www.twitch.tv/forjadoresdeleyendas',
  },
};

export function waLink(message: string): string {
  return `https://wa.me/${SITE.whatsapp}?text=${encodeURIComponent(message)}`;
}
```

- [ ] **Step 2: Crear Analytics.astro y .env.example**

`.env.example`:
```
# IDs pendientes — pedir a Armando. Sin ID, el script no se emite.
PUBLIC_GA4_ID=
PUBLIC_CLARITY_ID=
```

`src/components/Analytics.astro`:
```astro
---
const ga4 = import.meta.env.PUBLIC_GA4_ID;
const clarity = import.meta.env.PUBLIC_CLARITY_ID;
---
{ga4 && (
  <>
    <script async src={`https://www.googletagmanager.com/gtag/js?id=${ga4}`}></script>
    <script is:inline define:vars={{ ga4 }}>
      window.dataLayer = window.dataLayer || [];
      function gtag(){dataLayer.push(arguments);}
      gtag('js', new Date());
      gtag('config', ga4);
    </script>
  </>
)}
{clarity && (
  <script is:inline define:vars={{ clarity }}>
    (function(c,l,a,r,i,t,y){
      c[a]=c[a]||function(){(c[a].q=c[a].q||[]).push(arguments)};
      t=l.createElement(r);t.async=1;t.src="https://www.clarity.ms/tag/"+i;
      y=l.getElementsByTagName(r)[0];y.parentNode.insertBefore(t,y);
    })(window, document, "clarity", "script", clarity);
  </script>
)}
```

- [ ] **Step 3: Crear WhatsAppButton.astro**

```astro
---
import { waLink } from '@/lib/site';
interface Props {
  message: string;
  label: string;
  variant?: 'primary' | 'outline';
}
const { message, label, variant = 'primary' } = Astro.props;
const styles =
  variant === 'primary'
    ? 'bg-fire text-parchment-light hover:bg-amber hover:text-ink'
    : 'border-2 border-fire text-fire hover:bg-fire hover:text-parchment-light';
---
<a
  href={waLink(message)}
  target="_blank"
  rel="noopener"
  class={`inline-block rounded-lg px-6 py-3 font-display font-bold tracking-wide transition-colors ${styles}`}
>
  {label}
</a>
```

- [ ] **Step 4: Crear Header.astro y Footer.astro**

`Header.astro` — logo (escudo + nombre en Cinzel, NUNCA el letrero IA con typo) y nav: Inicio, El Banquete (`/#banquete`), Precios (`/#precios`), FAQ (`/faq`), Crónicas (`/cronicas`):
```astro
---
import { Image } from 'astro:assets';
import escudo from '@/assets/img/escudo.png';
const nav = [
  { href: '/', label: 'Inicio' },
  { href: '/#banquete', label: 'El Banquete' },
  { href: '/#precios', label: 'Precios' },
  { href: '/faq', label: 'FAQ' },
  { href: '/cronicas', label: 'Crónicas' },
];
---
<header class="sticky top-0 z-50 bg-ink/95 text-parchment-light backdrop-blur">
  <div class="mx-auto flex max-w-6xl items-center justify-between px-4 py-3">
    <a href="/" class="flex items-center gap-3">
      <Image src={escudo} alt="Escudo de Forjadores de Leyendas" width={48} loading="eager" />
      <span class="font-display text-lg font-bold tracking-wide">Forjadores de Leyendas</span>
    </a>
    <nav aria-label="Principal">
      <ul class="flex gap-5 text-sm">
        {nav.map((item) => (
          <li><a href={item.href} class="hover:text-amber transition-colors">{item.label}</a></li>
        ))}
      </ul>
    </nav>
  </div>
</header>
```
Nota: `src/assets/img/escudo.png` se copia en esta task desde `brand/originales/forjadoresleyendas.jpg` NO — ese es JPG con fondo. Usar `brand/originales/Forjadores de Leyendasfondotransparente (2).png` recortado al escudo (sin el letrero con typo). Comando:
```bash
mkdir -p src/assets/img
# El PNG transparente completo incluye el letrero con typo abajo; recortar solo el escudo (mitad superior ~68%)
npx sharp-cli --input "brand/originales/Forjadores de Leyendasfondotransparente (2).png" --output src/assets/img/escudo.png extract 0 0 1024 700 2>/dev/null || node -e "
const sharp = require('sharp');
sharp('brand/originales/Forjadores de Leyendasfondotransparente (2).png')
  .extract({ left: 130, top: 60, width: 700, height: 660 })
  .png().toFile('src/assets/img/escudo.png').then(() => console.log('ok'));
"
```
Verificar visualmente (Read del PNG resultante) que el recorte contiene el escudo completo y NADA del letrero "FORJADRES". Ajustar coordenadas si hace falta.

`Footer.astro` — redes (texto + aria-label, sin librería de iconos), dirección, WhatsApp, copyright:
```astro
---
import { SITE, waLink } from '@/lib/site';
const socials = [
  ['Instagram', SITE.social.instagram],
  ['YouTube', SITE.social.youtube],
  ['X', SITE.social.x],
  ['TikTok', SITE.social.tiktok],
  ['Twitch', SITE.social.twitch],
];
---
<footer class="bg-ink text-parchment-light">
  <div class="mx-auto grid max-w-6xl gap-8 px-4 py-12 sm:grid-cols-3">
    <div>
      <h2 class="font-display text-xl text-amber">Forjadores de Leyendas</h2>
      <p class="mt-2 text-sm opacity-80">Tu aventura comienza aquí… ¿Estás listo para lanzar el dado?</p>
    </div>
    <div>
      <h3 class="font-display text-amber">La Posada</h3>
      <p class="mt-2 text-sm">{SITE.address.street}, {SITE.address.neighborhood}<br />{SITE.address.city}, {SITE.address.state}</p>
      <a href={waLink('Hola, quiero información sobre Forjadores de Leyendas.')} class="mt-2 inline-block text-sm text-amber hover:underline">WhatsApp: {SITE.phone}</a>
    </div>
    <div>
      <h3 class="font-display text-amber">Síguenos</h3>
      <ul class="mt-2 space-y-1 text-sm">
        {socials.map(([name, url]) => (
          <li><a href={url} target="_blank" rel="noopener" class="hover:text-amber">{name}</a></li>
        ))}
      </ul>
    </div>
  </div>
  <p class="border-t border-wood py-4 text-center text-xs opacity-60">© {new Date().getFullYear()} Forjadores de Leyendas · Puebla, México</p>
</footer>
```

- [ ] **Step 5: Crear BaseLayout.astro**

```astro
---
import '@/styles/global.css';
import '@fontsource/cinzel/400.css';
import '@fontsource/cinzel/700.css';
import '@fontsource-variable/inter';
import Header from '@/components/Header.astro';
import Footer from '@/components/Footer.astro';
import Analytics from '@/components/Analytics.astro';
import { SITE } from '@/lib/site';

interface Props {
  title: string;
  description: string;
  ogImage?: string;
}
const { title, description, ogImage = '/og-default.jpg' } = Astro.props;
const canonical = new URL(Astro.url.pathname, SITE.url).href;
---
<!doctype html>
<html lang="es-MX">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{title}</title>
    <meta name="description" content={description} />
    <link rel="canonical" href={canonical} />
    <link rel="icon" href="/favicon.svg" type="image/svg+xml" />
    <link rel="sitemap" href="/sitemap-index.xml" />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:image" content={new URL(ogImage, SITE.url).href} />
    <meta property="og:type" content="website" />
    <meta property="og:locale" content="es_MX" />
    <meta name="twitter:card" content="summary_large_image" />
    <Analytics />
  </head>
  <body class="flex min-h-screen flex-col">
    <Header />
    <main class="flex-1"><slot /></main>
    <Footer />
  </body>
</html>
```

`public/favicon.svg`: un d20 simple (hexágono con "20") en los colores de la marca:
```svg
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100"><polygon points="50,4 92,27 92,73 50,96 8,73 8,27" fill="#c4622d" stroke="#2b1f14" stroke-width="4"/><text x="50" y="62" font-family="serif" font-weight="bold" font-size="34" fill="#faf3e3" text-anchor="middle">20</text></svg>
```

- [ ] **Step 6: Verificar build y commit**

```bash
npm run build && grep -q 'og:title' dist/index.html || echo "FALTA integrar layout (se integra en Task 4, OK si index sigue provisional)"
git add -A && git commit -m "feat: layout base, header/footer, WhatsApp CTA y analytics condicionales"
```

---

### Task 3: Generación de imágenes con Nano Banana (API Gemini)

**Files:**
- Create: `scripts/generate-images.mjs`, `src/assets/img/*.jpg` (generadas), `public/og-default.jpg`

**Interfaces:**
- Consumes: `GEMINI_API_KEY` en `.env` (⚠️ si no existe, DETENERSE y pedirla a Armando antes de continuar).
- Produces: imágenes en `src/assets/img/`: `hero-taberna.jpg`, `guardian.jpg`, `mesa-juego.jpg`, `banquete.jpg`, `escenografia.jpg`, `novatos.jpg`, y `public/og-default.jpg` (1200×630).

- [ ] **Step 1: Verificar API key**

```bash
grep -q '^GEMINI_API_KEY=.\+' .env && echo OK || echo "FALTA GEMINI_API_KEY — pedir a Armando"
```
Si falta: detener la task, avisar. Las demás tasks pueden continuar con las imágenes pendientes (usar `hero-taberna.jpg` faltante rompe el build de Task 4 — en ese caso, generar primero).

- [ ] **Step 2: Escribir scripts/generate-images.mjs**

Estilo unificado en todos los prompts para coherencia visual (el problema del sitio viejo era la mezcla de estilos):

```js
import { readFileSync, writeFileSync, mkdirSync } from 'node:fs';

const KEY = process.env.GEMINI_API_KEY ?? readFileSync('.env', 'utf8').match(/^GEMINI_API_KEY=(.+)$/m)?.[1];
if (!KEY) throw new Error('GEMINI_API_KEY no configurada');

const STYLE =
  'Warm digital fantasy illustration, painterly, cozy medieval tavern aesthetic, amber candlelight, rich browns and parchment tones, high detail, no text, no watermark, no people looking at camera';

const IMAGES = [
  ['hero-taberna', `Interior of a cozy medieval fantasy tavern prepared for a tabletop role-playing session: long wooden table with dice, maps and miniatures, lanterns and candles, shelves with books and potions. ${STYLE}. Wide 16:9 composition.`],
  ['guardian', `Charismatic fantasy innkeeper-storyteller behind a game master screen, gesturing dramatically while telling a story, warm candlelight, players' silhouettes in foreground. ${STYLE}.`],
  ['mesa-juego', `Close-up of a tabletop RPG table: a hand rolling a glowing d20 die, character sheets, painted miniatures on a battle map, mugs of drink. ${STYLE}.`],
  ['banquete', `Rustic fantasy tavern feast on a wooden table: hearty burgers, sausages, golden fries, mugs of honey mead, roasted chicken wings, medieval presentation. ${STYLE}.`],
  ['escenografia', `Immersive game room with digital screen showing a fantasy map, themed medieval decoration, controlled ambient lighting, table set for adventurers. ${STYLE}.`],
  ['novatos', `Group of diverse young friends laughing around a tabletop RPG table, welcoming atmosphere, one of them rolling dice for the first time, warm tavern light. ${STYLE}.`],
];

const MODELS = ['gemini-3-pro-image-preview', 'gemini-2.5-flash-image'];

async function generate(name, prompt) {
  for (const model of MODELS) {
    const res = await fetch(
      `https://generativelanguage.googleapis.com/v1beta/models/${model}:generateContent?key=${KEY}`,
      {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ contents: [{ parts: [{ text: prompt }] }] }),
      },
    );
    if (!res.ok) { console.warn(`${model} → HTTP ${res.status}, probando siguiente`); continue; }
    const data = await res.json();
    const part = data.candidates?.[0]?.content?.parts?.find((p) => p.inlineData);
    if (!part) { console.warn(`${model} → sin imagen, probando siguiente`); continue; }
    writeFileSync(`src/assets/img/${name}.jpg`, Buffer.from(part.inlineData.data, 'base64'));
    console.log(`✔ ${name} (${model})`);
    return;
  }
  throw new Error(`No se pudo generar ${name}`);
}

mkdirSync('src/assets/img', { recursive: true });
for (const [name, prompt] of IMAGES) await generate(name, prompt);
```

- [ ] **Step 3: Ejecutar y revisar**

```bash
node scripts/generate-images.mjs
```
Expected: 6 imágenes en `src/assets/img/`. **Revisar cada una visualmente (Read)**: estilo coherente, sin texto incrustado, sin deformidades. Regenerar las que fallen (basta re-ejecutar; el script sobreescribe).

- [ ] **Step 4: Crear OG image**

```bash
node -e "
const sharp = require('sharp');
sharp('src/assets/img/hero-taberna.jpg').resize(1200, 630, { fit: 'cover' }).jpeg({ quality: 82 }).toFile('public/og-default.jpg').then(() => console.log('ok'));
"
```

- [ ] **Step 5: Commit**

```bash
git add -A && git commit -m "feat: set de imágenes coherente generado con Nano Banana + OG image"
```

---

### Task 4: Página de inicio completa (copy + secciones)

**Files:**
- Create: `src/pages/index.astro`, `src/components/SectionTitle.astro`, `src/data/menu.ts`, `src/data/precios.ts`
- Delete: contenido provisional de `index.astro`

**Interfaces:**
- Consumes: `BaseLayout`, `WhatsAppButton`, `SITE`, imágenes de Task 3.
- Produces: `/#banquete` y `/#precios` como anclas (las usa el Header de Task 2). `src/data/menu.ts` exporta `MENU: { nombre: string; descripcion: string; precio?: string }[]`; `src/data/precios.ts` exporta `SESION` y `CAMPANAS`.

- [ ] **Step 1: Crear datos (menú y precios)**

`src/data/precios.ts` (precios verbatim de la FAQ de Pollo):
```ts
export const SESION = {
  titulo: 'Sesión suelta (one-shot)',
  precio: '$300 – $500 MXN',
  unidad: 'por persona',
  incluye: [
    'Guardián de Historias (Dungeon Master) experto',
    'Escenografía digital e interactiva',
    'Comida y bebida temática',
    'Dados, miniaturas y accesorios de juego',
  ],
};

export const CAMPANAS = [
  { nombre: 'Campaña corta', sesiones: 4, precio: '$1,800 MXN' },
  { nombre: 'Campaña media', sesiones: 6, precio: '$2,400 MXN' },
  { nombre: 'Campaña larga', sesiones: 10, precio: '$3,500 MXN' },
];
```

`src/data/menu.ts` (menú ficticio de temporada — libertad creativa aprobada; corregir cuando Pollo confirme el real):
```ts
// Menú de temporada: platillos ilustrativos aprobados por Armando hasta tener el menú real de Pollo.
export const MENU = [
  { nombre: 'Hamburguesa del Vikingo', descripcion: 'Doble carne, queso fundido y cebolla caramelizada. Digna de un salón de guerreros.' },
  { nombre: 'Hot Dog del Aventurero', descripcion: 'Salchicha jumbo con tocino, para reponer fuerzas entre mazmorras.' },
  { nombre: 'Alitas del Dragón', descripcion: 'Bañadas en salsa de fuego. Solo para valientes con constitución alta.' },
  { nombre: 'Nachos de la Cripta', descripcion: 'Montaña de totopos con queso, frijol y jalapeño. Botín para compartir.' },
  { nombre: 'Hidromiel de la Posada', descripcion: 'Nuestra bebida insignia de miel especiada, servida en tarro.' },
  { nombre: 'Elixir del Mago', descripcion: 'Limonada mística de temporada que restaura puntos de maná.' },
  { nombre: 'Café del Cronista', descripcion: 'De olla, para las sesiones que se alargan hasta la madrugada.' },
  { nombre: 'Brownie de la Mazmorra', descripcion: 'Oscuro, denso y con tesoro de nuez adentro. La recompensa final.' },
];
```

- [ ] **Step 2: Crear SectionTitle.astro**

```astro
---
interface Props { eyebrow?: string; title: string }
const { eyebrow, title } = Astro.props;
---
<div class="mx-auto mb-10 max-w-2xl text-center">
  {eyebrow && <p class="mb-1 text-sm font-bold uppercase tracking-widest text-fire">{eyebrow}</p>}
  <h2 class="font-display text-3xl font-bold text-wood sm:text-4xl">{title}</h2>
</div>
```

- [ ] **Step 3: Escribir index.astro (estructura + copy completo)**

Secciones en orden, con este copy (redactado con la voz de marca; ajustar detalles menores está bien, cambiar el mensaje no):

1. **Hero** (bg `hero-taberna.jpg` con overlay oscuro, texto en parchment-light):
   - H1: `Cada tirada de dados forja una leyenda`
   - Sub: `Sesiones privadas de Dungeons & Dragons y juegos de rol en una posada temática en el corazón de Puebla. Con comida, bebida y un Guardián de Historias que hará tu aventura inolvidable.`
   - CTA primario: WhatsAppButton `message="¡Saludos, Guardián! Quiero reservar una sesión de rol en la posada. 🎲"` `label="Reserva tu aventura"`
   - CTA secundario (outline, ancla): `Ver precios` → `/#precios`
2. **¿Nunca has jugado rol?** (3 pasos, imagen `novatos.jpg`):
   - Intro: `¿Nunca has jugado rol? Perfecto: las mejores leyendas empiezan en el nivel 1.`
   - Paso 1 — `Reúne a tu grupo`: `De 3 a 6 aventureros. Amigos, familia o tu equipo de trabajo.`
   - Paso 2 — `Elige tu destino`: `Escríbenos por WhatsApp y cuéntanos qué aventura buscan. Nosotros preparamos todo: reglas, dados, historia.`
   - Paso 3 — `Lanza el dado`: `Llega a la posada y déjate llevar. Tu Guardián de Historias se encarga de que tu primera partida sea legendaria.`
3. **Qué incluye tu sesión** (4 tarjetas con imágenes `guardian.jpg`, `escenografia.jpg`, `banquete.jpg`, `mesa-juego.jpg`):
   - `Guardián de Historias` / `Más que un Dungeon Master: un narrador experto que adapta la aventura a tu grupo y convierte cada decisión en un momento épico.`
   - `Escenografía inmersiva` / `Mapas digitales interactivos, iluminación y ambientación que te meten de lleno en la historia.`
   - `Banquete incluido` / `Comida y bebida temática servida en plena aventura. Ningún héroe pelea con el estómago vacío.`
   - `Todo el equipo` / `Dados, miniaturas, hojas de personaje y accesorios. Solo trae tu imaginación.`
4. **Precios** (`id="precios"`, datos de `precios.ts`): tarjeta de sesión suelta + 3 de campañas, nota `Reserva con 48 h de anticipación. Cambios de fecha sin costo hasta 72 h antes.` CTA WhatsApp `message="Hola, quiero reservar una campaña de rol. ¿Me cuentas los detalles? ⚔️"` `label="Arma tu campaña"`.
5. **El Banquete** (`id="banquete"`, datos de `menu.ts`, imagen `banquete.jpg`): grid de platillos + leyenda `Menú de temporada — cambia según la cosecha y el humor del posadero.`
6. **Ubicación**: dirección completa, iframe de Google Maps embed (`https://www.google.com/maps?q=C.+12+Sur+908-1,+Barrio+de+Analco,+Puebla&output=embed`, `loading="lazy"`, `title="Mapa de la posada"`), texto `A unos pasos del centro histórico de Puebla, en el Barrio de Analco.`
7. **CTA final** (bg ink): H2 `¿Listo para comenzar tu aventura?` + `La posada tiene una mesa esperando a tu grupo. Que los dados decidan el resto.` + WhatsAppButton reserva.
8. **JSON-LD LocalBusiness** en un `<script type="application/ld+json" is:inline>` con `set:html`:
```json
{
  "@context": "https://schema.org",
  "@type": "Restaurant",
  "name": "Forjadores de Leyendas",
  "description": "Posada temática con sesiones privadas de Dungeons & Dragons y juegos de rol en Puebla.",
  "servesCuisine": "Comida temática de fantasía",
  "url": "https://forjadoresdeleyendas.mx",
  "telephone": "+522221890232",
  "address": {
    "@type": "PostalAddress",
    "streetAddress": "C. 12 Sur 908-1, Barrio de Analco",
    "addressLocality": "Puebla",
    "addressRegion": "Puebla",
    "postalCode": "72500",
    "addressCountry": "MX"
  },
  "sameAs": [
    "https://x.com/Forjadleyendas",
    "https://www.youtube.com/@ForjadoresdeLeyendas",
    "https://www.instagram.com/forjadoresdeleyendas/",
    "https://www.tiktok.com/@forjadoresdeleyendas_",
    "https://www.twitch.tv/forjadoresdeleyendas"
  ],
  "hasMenu": {
    "@type": "Menu",
    "name": "El Banquete (menú de temporada)",
    "hasMenuSection": {
      "@type": "MenuSection",
      "name": "Menú de temporada",
      "hasMenuItem": "<generar desde MENU: { '@type': 'MenuItem', name, description } por cada platillo>"
    }
  }
}
```
El bloque `hasMenuItem` NO se escribe a mano: construir el objeto en el frontmatter de `index.astro` con `MENU.map((m) => ({ '@type': 'MenuItem', name: m.nombre, description: m.descripcion }))` y serializar todo el JSON-LD con `JSON.stringify` (igual que en la FAQ).

Implementación: usar `<Image>` de `astro:assets` para todas las imágenes locales (optimización automática), headings en `font-display text-wood`, secciones alternando `bg-parchment` / `bg-parchment-light`, `max-w-6xl mx-auto px-4 py-16` por sección. Título de página: `Forjadores de Leyendas — Sesiones de rol y D&D en Puebla`. Description: la de `SITE.description`.

- [ ] **Step 4: Verificar build + contenido**

```bash
npm run build
grep -c 'wa.me/522221890232' dist/index.html   # Expected: >= 3
grep -q 'Hamburguesa del Vikingo' dist/index.html && echo MENU-OK
grep -q '"@type":"Restaurant"' dist/index.html && echo SCHEMA-OK
grep -qi 'FORJADRES' dist/index.html && echo "❌ TYPO DETECTADO" || echo TYPO-LIBRE
```

- [ ] **Step 5: Revisión visual**

Abrir con el Browser pane (`preview_start` con launch.json `npm run dev`, puerto 4321). Verificar: hero legible, sin overflow horizontal en 375px (`resize_window` preset mobile), anclas `#precios` y `#banquete` funcionan, botones WhatsApp abren `wa.me` con mensaje.

- [ ] **Step 6: Commit**

```bash
git add -A && git commit -m "feat: página de inicio completa con copy de marca, precios, banquete y schema"
```

---

### Task 5: Página FAQ con schema FAQPage

**Files:**
- Create: `src/pages/faq.astro`, `src/data/faq.ts`

**Interfaces:**
- Consumes: `BaseLayout`, `WhatsAppButton`.
- Produces: `/faq` con todas las preguntas del documento de Pollo y JSON-LD FAQPage generado desde `src/data/faq.ts` (`FAQ: { categoria: string; items: { q: string; a: string }[] }[]`).

- [ ] **Step 1: Crear src/data/faq.ts**

Transcribir TODO el contenido de `brand/originales/FAQ preguntas frecuentes.docx` (extraer con `textutil -convert txt -stdout`), reescrito con voz de marca pero sin cambiar datos (precios, políticas, tiempos). Estructura:
```ts
export const FAQ = [
  {
    categoria: 'Sesiones y precios',
    items: [
      { q: '¿Cuánto cuesta una sesión de rol?', a: 'Una sesión suelta (one-shot) cuesta entre $300 y $500 MXN por persona e incluye Guardián de Historias, ambientación, comida y bebida. También tenemos paquetes de campaña: corta de 4 sesiones por $1,800 MXN, media de 6 sesiones por $2,400 MXN y larga de 10 sesiones por $3,500 MXN por persona.' },
      { q: '¿Qué incluye la sesión?', a: 'Un Guardián de Historias (Dungeon Master) experto, escenografía digital e interactiva, comida y bebida, y acceso a dados, miniaturas y accesorios de juego. Tú solo trae a tu grupo y tus ganas de aventura.' },
      { q: '¿Cuántas personas pueden participar?', a: 'Las sesiones son para grupos de 3 a 6 aventureros.' },
      { q: '¿Puedo unirme si nunca he jugado rol?', a: '¡Claro! Nuestros Guardianes de Historias te guían desde la primera tirada. No necesitas saber nada antes de llegar: las reglas se aprenden jugando.' },
    ],
  },
  {
    categoria: 'Reservaciones y pagos',
    items: [
      { q: '¿Cómo puedo reservar una sesión?', a: 'Escríbenos por WhatsApp o por cualquiera de nuestras redes sociales y un Guardián te ayudará a elegir fecha y aventura.' },
      { q: '¿Con cuánto tiempo debo reservar?', a: 'Con al menos 48 horas de anticipación: preparamos los alimentos con productos frescos y diseñamos el one-shot para tu grupo.' },
      { q: '¿Qué métodos de pago aceptan?', a: 'Efectivo, tarjetas de crédito o débito, transferencias bancarias y PayPal.' },
      { q: '¿Puedo cambiar la fecha de mi sesión?', a: 'Sí, sin penalización si nos avisas con al menos 72 horas de anticipación.' },
      { q: '¿Cuál es la política de cancelaciones?', a: 'Cancelando con 72 horas de anticipación no hay penalización. Con menos de 72 horas, la penalización es de $250 MXN por persona.' },
      { q: '¿Tienen descuentos o membresías?', a: 'Por ahora no manejamos membresías, pero lanzamos promociones por temporada. Síguenos en redes para enterarte primero.' },
      { q: '¿Puedo reservar solo la sesión sin comida?', a: 'Sí. Déjanos tus datos y un Guardián de Historias de mayor rango coordinará los detalles contigo.' },
      { q: '¿Puedo reservar solo el espacio sin una sesión?', a: 'Sí, coordinándolo con un Guardián de Historias de mayor rango. Escríbenos y lo armamos.' },
    ],
  },
  {
    categoria: 'Extras y productos',
    items: [
      { q: '¿Qué productos adicionales venden?', a: 'Miniaturas 3D personalizadas ($250 – $500 MXN), dibujo épico de tu party ($500 – $3,500 MXN) y cosplay por pedido con precio según diseño.' },
      { q: '¿Se pueden personalizar las experiencias de rol?', a: '¡Sí! Si tienes una idea para una historia especial, la diseñamos a la medida de tu grupo.' },
      { q: '¿Los alimentos y bebidas están incluidos?', a: 'Sí, cada sesión incluye comida y bebida. También puedes pedir extras del menú de la posada.' },
    ],
  },
  {
    categoria: 'Contacto',
    items: [
      { q: '¿Cuáles son los canales de comunicación?', a: 'WhatsApp y nuestras redes sociales (Instagram, X, YouTube, TikTok y Twitch). Respondemos lo más rápido posible.' },
      { q: '¿Tienen eventos especiales?', a: 'Sí: noches temáticas, sesiones especiales y colaboraciones con creadores geek. Anunciamos todo en redes y en nuestras Crónicas.' },
      { q: '¿Tienen programa de referidos?', a: 'Estamos forjando uno: si traes a un amigo, ambos obtendrán 10% de descuento en su próxima sesión. Pregunta por WhatsApp si ya está activo.' },
    ],
  },
];
```

- [ ] **Step 2: Crear faq.astro**

```astro
---
import BaseLayout from '@/layouts/BaseLayout.astro';
import WhatsAppButton from '@/components/WhatsAppButton.astro';
import { FAQ } from '@/data/faq';

const jsonLd = JSON.stringify({
  '@context': 'https://schema.org',
  '@type': 'FAQPage',
  mainEntity: FAQ.flatMap((c) =>
    c.items.map((i) => ({
      '@type': 'Question',
      name: i.q,
      acceptedAnswer: { '@type': 'Answer', text: i.a },
    })),
  ),
});
---
<BaseLayout
  title="Preguntas frecuentes — Forjadores de Leyendas"
  description="Precios, reservaciones, políticas y todo lo que necesitas saber antes de tu sesión de rol en la posada."
>
  <div class="mx-auto max-w-3xl px-4 py-16">
    <h1 class="font-display text-4xl font-bold text-wood">Preguntas frecuentes</h1>
    <p class="mt-2 opacity-80">Todo lo que un aventurero debe saber antes de cruzar la puerta de la posada.</p>
    {FAQ.map((cat) => (
      <section class="mt-10">
        <h2 class="font-display text-2xl text-fire">{cat.categoria}</h2>
        <div class="mt-4 space-y-3">
          {cat.items.map((item) => (
            <details class="rounded-lg bg-parchment-light p-4 shadow-sm">
              <summary class="cursor-pointer font-bold">{item.q}</summary>
              <p class="mt-2 opacity-90">{item.a}</p>
            </details>
          ))}
        </div>
      </section>
    ))}
    <div class="mt-12 text-center">
      <p class="mb-4 font-display text-xl">¿Te quedó alguna duda?</p>
      <WhatsAppButton message="Hola, tengo una pregunta sobre las sesiones de rol. 🤔" label="Pregúntale al posadero" />
    </div>
  </div>
  <script type="application/ld+json" set:html={jsonLd} is:inline />
</BaseLayout>
```

- [ ] **Step 3: Verificar y commit**

```bash
npm run build
grep -q '"@type":"FAQPage"' dist/faq/index.html && echo SCHEMA-OK
grep -c '<details' dist/faq/index.html   # Expected: 18
git add -A && git commit -m "feat: página FAQ completa con schema FAQPage"
```

---

### Task 6: Blog "Crónicas de la Posada"

**Files:**
- Create: `src/content.config.ts`, `src/content/cronicas/bienvenida-a-la-posada.md`, `src/content/cronicas/guia-primera-sesion-rol.md`, `src/pages/cronicas/index.astro`, `src/pages/cronicas/[slug].astro`

**Interfaces:**
- Consumes: `BaseLayout`.
- Produces: colección `cronicas` con frontmatter `{ title: string; description: string; pubDate: Date; categoria: 'cronicas' | 'aliados'; cover?: image }`. Rutas `/cronicas` y `/cronicas/<slug>`.

- [ ] **Step 1: Definir la colección**

`src/content.config.ts`:
```ts
import { defineCollection, z } from 'astro:content';
import { glob } from 'astro/loaders';

const cronicas = defineCollection({
  loader: glob({ pattern: '**/*.md', base: './src/content/cronicas' }),
  schema: z.object({
    title: z.string(),
    description: z.string(),
    pubDate: z.coerce.date(),
    categoria: z.enum(['cronicas', 'aliados']).default('cronicas'),
  }),
});

export const collections = { cronicas };
```

- [ ] **Step 2: Escribir las 2 entradas iniciales**

Entrada 1 — `bienvenida-a-la-posada.md` (anuncio, sin hechos inventados):
```markdown
---
title: 'La posada abre sus puertas'
description: 'Bienvenido a Forjadores de Leyendas: qué somos, qué vas a vivir aquí y por qué tu próxima historia épica empieza en Puebla.'
pubDate: 2026-07-30
categoria: cronicas
---

Toda posada tiene una primera noche...
```
Cuerpo (~300 palabras, voz del posadero): qué es Forjadores de Leyendas, qué se vive en una sesión, invitación a reservar. Cerrar con CTA a WhatsApp en Markdown: `[Reserva tu lugar en la posada](https://wa.me/522221890232?text=...)`.

Entrada 2 — `guia-primera-sesion-rol.md` (evergreen para SEO/novatos):
```markdown
---
title: 'Tu primera sesión de rol: guía para aventureros de nivel 1'
description: 'Nunca has jugado D&D y no sabes qué esperar. Esta guía del posadero te cuenta cómo es una sesión, qué llevar y por qué no necesitas saber reglas.'
pubDate: 2026-07-30
categoria: cronicas
---
```
Cuerpo (~500 palabras): qué es un juego de rol explicado sin jerga, cómo es la sesión paso a paso en la posada (llegas, comes, el Guardián narra, tú decides), mitos comunes ("es muy complicado", "necesito estudiar reglas"), qué llevar (nada, todo incluido). Escribir contenido real y completo, no esqueleto.

**NO** inventar entradas de la categoría `aliados` — esas esperan material real de Pollo.

- [ ] **Step 3: Crear index y página de entrada**

`src/pages/cronicas/index.astro`:
```astro
---
import BaseLayout from '@/layouts/BaseLayout.astro';
import { getCollection } from 'astro:content';
const posts = (await getCollection('cronicas')).sort(
  (a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf(),
);
const labels = { cronicas: 'Crónica', aliados: 'Aliados' };
---
<BaseLayout
  title="Crónicas de la Posada — Forjadores de Leyendas"
  description="Historias, sesiones y aventuras registradas por el posadero: crónicas de la comunidad rolera y de nuestros aliados en todo el país."
>
  <div class="mx-auto max-w-3xl px-4 py-16">
    <h1 class="font-display text-4xl font-bold text-wood">Crónicas de la Posada</h1>
    <p class="mt-2 opacity-80">Aquí el posadero registra las gestas que nacen en estas mesas.</p>
    <div class="mt-10 space-y-6">
      {posts.map((post) => (
        <a href={`/cronicas/${post.id}/`} class="block rounded-lg bg-parchment-light p-6 shadow-sm transition-shadow hover:shadow-md">
          <p class="text-xs font-bold uppercase tracking-widest text-fire">{labels[post.data.categoria]}</p>
          <h2 class="mt-1 font-display text-2xl text-wood">{post.data.title}</h2>
          <p class="mt-2 text-sm opacity-80">{post.data.description}</p>
          <time class="mt-2 block text-xs opacity-60" datetime={post.data.pubDate.toISOString()}>
            {post.data.pubDate.toLocaleDateString('es-MX', { dateStyle: 'long' })}
          </time>
        </a>
      ))}
    </div>
  </div>
</BaseLayout>
```

`src/pages/cronicas/[slug].astro`:
```astro
---
import BaseLayout from '@/layouts/BaseLayout.astro';
import { getCollection, render } from 'astro:content';

export async function getStaticPaths() {
  const posts = await getCollection('cronicas');
  return posts.map((post) => ({ params: { slug: post.id }, props: { post } }));
}
const { post } = Astro.props;
const { Content } = await render(post);
---
<BaseLayout title={`${post.data.title} — Crónicas de la Posada`} description={post.data.description}>
  <article class="prose prose-stone mx-auto max-w-3xl px-4 py-16">
    <h1 class="font-display text-wood">{post.data.title}</h1>
    <Content />
  </article>
</BaseLayout>
```
Nota: si `prose` no está disponible (plugin typography no instalado), en su lugar estilar con clases globales en `global.css`: `article h2 { @apply font-display text-2xl text-wood mt-8 }`, `article p { @apply mt-4 leading-relaxed }`. NO instalar @tailwindcss/typography solo para esto si con 4 reglas basta.

- [ ] **Step 4: Verificar y commit**

```bash
npm run build
ls dist/cronicas/index.html dist/cronicas/guia-primera-sesion-rol/index.html
git add -A && git commit -m "feat: blog Crónicas de la Posada con 2 entradas iniciales"
```

---

### Task 7: 404, robots, IndexNow y verificación Bing

**Files:**
- Create: `src/pages/404.astro`, `public/robots.txt`, `public/<indexnow-key>.txt`, `README.md`

**Interfaces:**
- Consumes: `BaseLayout`, `WhatsAppButton`.
- Produces: 404 temática; robots.txt apuntando al sitemap; key de IndexNow servida en la raíz; README con pendientes documentados.

- [ ] **Step 1: 404 temática**

`src/pages/404.astro`:
```astro
---
import BaseLayout from '@/layouts/BaseLayout.astro';
---
<BaseLayout title="Tirada fallida — Forjadores de Leyendas" description="Esta página no existe. Vuelve a la posada.">
  <div class="mx-auto max-w-xl px-4 py-24 text-center">
    <p class="font-display text-7xl font-bold text-fire">1 🎲</p>
    <h1 class="mt-4 font-display text-3xl font-bold text-wood">Tirada fallida: crítico en contra</h1>
    <p class="mt-3 opacity-80">La página que buscas no existe en este plano de la realidad. Quizá un mago la desvaneció, o quizá el enlace estaba maldito.</p>
    <a href="/" class="mt-8 inline-block rounded-lg bg-fire px-6 py-3 font-display font-bold text-parchment-light transition-colors hover:bg-amber hover:text-ink">Volver a la posada</a>
  </div>
</BaseLayout>
```

- [ ] **Step 2: robots.txt e IndexNow**

`public/robots.txt`:
```
User-agent: *
Allow: /

Sitemap: https://forjadoresdeleyendas.mx/sitemap-index.xml
```

IndexNow — generar una key hex de 32 chars y crear el archivo con ese nombre:
```bash
KEY=$(openssl rand -hex 16)
echo "$KEY" > "public/${KEY}.txt"
echo "IndexNow key: $KEY"
```
Documentar la key en el README (Step 3). El ping a IndexNow (`https://api.indexnow.org/indexnow?url=...&key=...`) se hace tras cada deploy — instrucción en README, no automatizar aún (YAGNI hasta que haya publicaciones frecuentes).

- [ ] **Step 3: README con pendientes**

`README.md`: qué es el proyecto, comandos (`npm install`, `npm run dev`, `npm run build`), cómo publicar una entrada de blog (crear `.md` en `src/content/cronicas/`, frontmatter de ejemplo, push a main), y la tabla de pendientes:

```markdown
## Pendientes de configuración

| Pendiente | Cómo se resuelve |
|---|---|
| ID de Google Analytics 4 | Crear propiedad en analytics.google.com → poner `PUBLIC_GA4_ID=G-XXXX` en `.env` y en los secrets del repo |
| ID de Microsoft Clarity | clarity.microsoft.com → nuevo proyecto → `PUBLIC_CLARITY_ID=xxxx` |
| Verificación Bing Webmaster | bing.com/webmasters → importar desde Google Search Console o agregar meta tag en `BaseLayout.astro` |
| IndexNow | Key ya generada: `<KEY>` (archivo en `public/`). Tras publicar contenido: `curl "https://api.indexnow.org/indexnow?url=https://forjadoresdeleyendas.mx&key=<KEY>"` |
| Rama de deploy en Hostinger | Confirmar que Hostinger jala la rama `hostinger-deploy` (carpeta raíz = `public_html`) |
| Menú real del restaurante | Editar `src/data/menu.ts` cuando Pollo lo confirme |
| Google Search Console | Dar de alta la propiedad y enviar el sitemap |
```

- [ ] **Step 4: Verificar y commit**

```bash
npm run build && ls dist/404.html dist/robots.txt
git add -A && git commit -m "feat: 404 temática, robots.txt, IndexNow key y README"
```

---

### Task 8: GitHub Action de build y deploy a rama hostinger-deploy

**Files:**
- Create: `.github/workflows/deploy.yml`

**Interfaces:**
- Consumes: build de Astro (`npm run build` → `dist/`).
- Produces: rama `hostinger-deploy` con el contenido de `dist/` en su raíz, actualizada en cada push a `main`.

- [ ] **Step 1: Crear el workflow**

`.github/workflows/deploy.yml`:
```yaml
name: Build y deploy a Hostinger

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm run build
        env:
          PUBLIC_GA4_ID: ${{ vars.PUBLIC_GA4_ID }}
          PUBLIC_CLARITY_ID: ${{ vars.PUBLIC_CLARITY_ID }}
      - name: Publicar dist en rama hostinger-deploy
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist
          publish_branch: hostinger-deploy
```
Nota: los IDs de analytics van como *variables* del repo (Settings → Secrets and variables → Actions → Variables) para que el build de producción los incluya; en local `.env` queda vacío y no se emiten scripts.

- [ ] **Step 2: Push y verificar el Action**

```bash
git add -A && git commit -m "ci: build y publicación automática en rama hostinger-deploy"
git push origin main
gh run watch --repo armando86mx/forjadoresdeleyendas || gh run list --repo armando86mx/forjadoresdeleyendas -L 3
git ls-remote origin hostinger-deploy   # Expected: la rama existe tras el run
```

- [ ] **Step 3: Avisar a Armando**

Confirmar con Armando que Hostinger quede configurado para jalar la rama `hostinger-deploy` hacia `public_html`. Hasta entonces el WordPress actual sigue en producción — no hay riesgo.

---

### Task 9: QA final y revisión

**Files:** ninguno nuevo (correcciones que surjan).

- [ ] **Step 1: Build limpio y greps de regresión**

```bash
npm run build
grep -qi 'FORJADRES' -r dist/ && echo "❌ TYPO" || echo OK-TYPO
grep -q 'sitemap-index.xml' dist/robots.txt && echo OK-SITEMAP
ls dist/sitemap-index.xml dist/og-default.jpg dist/favicon.svg
```

- [ ] **Step 2: Revisión visual completa en Browser pane**

Home, FAQ, Crónicas, una entrada, 404 (URL inventada). En desktop (1280) y mobile (375): sin overflow horizontal, contraste legible en hero, imágenes cargando, anclas del header funcionando.

- [ ] **Step 3: Revisión de código y copy**

Usar la skill `superpowers:requesting-code-review` sobre el diff completo. Verificar copy: cero "FORJADRES", cero lorem ipsum, precios idénticos a la FAQ de Pollo.

- [ ] **Step 4: Commit final y push**

```bash
git add -A && git commit -m "fix: ajustes de QA final" || echo "nada que corregir"
git push origin main
```
